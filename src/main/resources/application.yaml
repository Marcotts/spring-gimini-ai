spring:
  application:
    name: spring-gemini-ai

  ai:
    model:
      chat: google-genai
    google:
      genai:
        api-key: ${GOOGLE_GENAI_API_KEY}
        chat:
          options: { }

server:
  port: 9191
